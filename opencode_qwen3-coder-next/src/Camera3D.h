#pragma once

#include <raylib.h>

// Raylib provides Camera3D, we just need a helper to set orientation
inline void SetCameraOrientation(float x, float y, float z, float rotation) {
    // Update this when we implement proper camera
}
